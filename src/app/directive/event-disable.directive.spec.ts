import { EventDisableDirective } from './event-disable.directive';

describe('EventDisableDirective', () => {
  it('should create an instance', () => {
    const directive = new EventDisableDirective();
    expect(directive).toBeTruthy();
  });
});
